head(data)
str(data)
unique(data$grp)
data_avg
stats
stats
stats
stats
stats
max(stats$rate)
500 / stats$rate - (500 / max(stats$rate))
stats
stats
data_sub
head(data_sub)
ascii(
ascii(stats, include.rownames = F)
ascii(stats, include.rownames = F, rownames = NULL)
ascii(stats, include.rownames = F, rownames = rep("", 6)
)
ascii(stats, include.rownames = F, rownames = rep("", 6), colnames = names(stats))
quit()
n
quit()
n
quit()
n
ggplot()
setwd("../")
quit()
n
quit()
n
